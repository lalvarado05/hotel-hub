<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Hotel Hub" %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>
    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <!-- Load CSS -->
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <!-- JavaScript Libraries -->
    <%= javascript_include_tag "https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.0/jquery.min.js", defer: true, crossorigin: "anonymous" %>
    <%= javascript_include_tag "https://cdn.datatables.net/1.11.5/js/jquery.dataTables.min.js", defer: true, crossorigin: "anonymous" %>
    <%= javascript_include_tag "https://cdn.datatables.net/1.11.5/js/dataTables.bootstrap5.min.js", defer: true, crossorigin: "anonymous" %>
    <!-- Import Map -->
    <%= javascript_importmap_tags %>
  </head>
  <body class="d-flex flex-column min-vh-100">
    <%= render "layouts/partials/header" %>
    <div class="container flex-grow-1">
      <!-- Flash Messages -->
      <div class="position-relative">
        <% flash.each do |key, value| %>
          <div class="alert alert-<%= key == 'notice' ? 'success' : 'danger' %> alert-dismissible fade show position-fixed" style="top: 20px; right: 20px; z-index: 1050;">
            <%= value %>
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
          </div>
        <% end %>
      </div>
      <!-- Main Content -->
      <%= yield %>
    </div>
    <%= render "layouts/partials/footer" %>
  </body>
</html>

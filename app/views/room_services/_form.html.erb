<%= form_with(model: room_service) do |form| %>
  <% if room_service.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(room_service.errors.count, "error") %> prohibited this room_service from being saved:</h2>

      <ul>
        <% room_service.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :room_id, style: "display: block" %>
    <%= form.text_field :room_id %>
  </div>

  <div>
    <%= form.label :service_id, style: "display: block" %>
    <%= form.text_field :service_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>

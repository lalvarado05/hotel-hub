<% content_for :title, "Editing Service" %>
<div class="container my-3">
  <div class="d-flex justify-content-between align-items-center mb-4">
    <h1 class="mb-0">Editing Service</h1>
    <%= link_to "Back to Services", services_path, class: "btn btn-secondary" %>
  </div>
  <div class="card shadow-sm" style="width: 400px; margin: 0 auto;">
    <div class="card-body">
      <%= render "form", service: @service %>
    </div>
  </div>
</div>
